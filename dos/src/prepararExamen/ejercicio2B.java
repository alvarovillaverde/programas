package prepararExamen;

/*
    Un grupo de inteligencia militar desea codificar los mensajes secretos utilizando un sistema que transforme
     las letras y números según las siguientes reglas:

    Todo mensaje debe ser convertido a mayúsculas.

    Reemplazar cada letra por la que sigue tres posiciones después en el abecedario. Si la letra es X, Y o Z, 
    se reemplaza por A, B o C, respectivamente (el abecedario es circular).

    Reemplazar cada dígito encontrado por el número que le sigue tres posiciones después en el sistema decimal. 
    Si el dígito es 7, 8 o 9, se reemplaza por 0, 1 o 2, respectivamente (el sistema decimal es circular).

    Los caracteres que no sean letras ni dígitos deben permanecer sin cambios.

    Escribe un programa en Java que solicite al usuario una cadena de texto (que puede contener letras, números 
    y caracteres especiales) y muestre el mensaje codificado siguiendo las reglas anteriores.
*/

public class ejercicio2B {

}
